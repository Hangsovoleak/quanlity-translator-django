[project]
name = "quanlity-translator-django"
version = "0.1.0"
description = "Django translator app"
requires-python = ">=3.12,<3.13"
dependencies = [
    "Django==5.2.9",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
